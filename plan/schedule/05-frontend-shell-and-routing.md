# 05 前端：站点壳与路由（在后端完成之后）

**实现状态**：**部分完成**。`write_index_html` 已生成最小 `public/index.html`（列表 + 链到 `posts/<id>/index.html`）；`typlog server` 可预览；文章页 **无** 统一页脚/返回链接；`base_url` **未** 用于链接前缀。

## 前置条件

- 01–04 已完成并通过验收（见 `00-index.md` 中「后端阶段完成判定」）。

## 目标

- 读者可通过 **首页** 找到文章列表，并进入 `public/posts/*.html`。
- 站内链接在 **本地预览路径** 与 **部署子路径**（若站点挂在 `/blog/`）下均正确——需尽早定 **base URL** 策略。

## 页面与路由（MVP）

- `public/index.html`：文章列表（标题、日期、链到各篇）。
- 可选：`public/archive.html` 或按年归档（第二迭代）。
- 每篇生成的 HTML：包含 **返回首页** 或站点标题链接（可在 Typst 模板或生成后注入二选一，需在实现时统一）。

## 静态资源

- 全局 CSS：如 `public/assets/css/site.css`。
- 与 02 中资源目录约定对齐，避免混用相对路径导致部署后断裂。

## 验收清单

- [x] 从首页可点击打开所有已发布文章（相对链接 `posts/<id>/index.html`）。
- [ ] 文章页可返回首页（需在 Typst 模板、构建后注入或统一壳中择一实现）。
- [ ] 至少一种部署场景（根路径或子路径）下链接无 404（依赖 `base_url` 策略与资源路径）。

## 时间估算（参考）

- MVP 壳 + 列表生成：**1～2 天**（若列表由脚本从元数据生成，视解析方式浮动）。
