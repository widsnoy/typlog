[package]
name = "typlog-core"
version = "0.1.0"
edition = "2024"
include = ["Cargo.toml", "src/**/*", "templates/**/*"]

[dependencies]
anyhow = "1"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
